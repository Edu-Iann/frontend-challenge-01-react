import React from 'react';
import { LoadingCircle, LoadingContainer } from './style';

const Loading = () => {
  return (
    <LoadingContainer>
      <LoadingCircle />
    </LoadingContainer>
  );
};

export default Loading;
